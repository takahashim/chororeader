# ChoroReader ローカル AI 層 仕様書（案）

local-ai-memo.md のたたき台を、現在の実装とこのアプリの思想に合わせて改訂したもの。

**この文書は案であり、スパイク（spikes/local-ai-embedding.md）を通過するまで本編（spec.md）には取り込まない。**
スパイクが立たなければ、この文書は全体を棚上げする。対象は macOS 版のみとする（第 1.4 節）。

> **スパイクの結果：GO（条件つき、2026-08-07）**
>
> 検索のために学習されたモデル（Ruri v3 130m）なら線を越えた。
> 課題 A 17/20（線は 14）、課題 B 12/15（線は 10）。
> OS が配るモデル（NLContextualEmbedding）では届かない（A 3/20、B 0/15）。
> 文の埋め込みではないため、長さが話題を上回る。詳しくは spikes/findings-local-ai.md。
>
> **第 4.6 節（OS が配るモデルで済ませる）は取り下げる。** 代わりに Ruri v3 を使う。
> 実装は Swift で自己完結できる（spikes/findings-swift-tokenizer.md、約 27 万トークンで
> 参照実装と食い違い 0）。kohagi は変換器と照合の参照として使う。
>
> 運用の数字も裏付けが取れた。ANE で 22 ms/単位、索引 6.1 MB／6,107 単位、
> 探索は総当たり 0.35 ms。ANN は要らない。

## 1. 位置付け

### 1.1 既にあるもの

たたき台の骨格である二段階検索は、既に本体の仕様であり実装である。

| たたき台の言葉 | 既にあるもの |
| --- | --- |
| Candidate Index → 原書検索 | 二字組索引（spec.md 第 10.4 節）。索引は候補を絞るだけで、当たりを決めない |
| Locator | 位置表現（spec.md 第 12.1 節）。飛び先・書き出し・割合の 3 段 |
| 書棚横断検索 | 実装済み。候補を絞ってから原書を走査し直す |
| 検索結果は原文断片を表示 | 実装済み。当たりの語の強調まで含む |

この文書が扱うのは、その上に足す**意味の層**だけである。

### 1.2 新しく作るもの

1. **関連箇所表示** — いま読んでいる場所に関連する、所有する他の書籍の箇所を出す
2. **意味検索** — 文字が一致しない箇所を、意味の近さで引く

1 が本命である。2 の看板の用例（「以前どこかの本で読んだが、どの本か分からない」）は、
特徴的な語を 1 つでも覚えていれば既存の横断検索で解ける。意味検索が効くのは
「概念しか覚えていない」場合に限られ、見た目より狭い。

### 1.3 作らないもの

たたき台にあって、この案から外したもの。理由とともに残す。

- **用語索引・コード検索・キーワード抽出** — 二字組索引が既に任意の 2 文字以上の語を引ける。
  横断検索の結果がそのまま「語 → 書籍と章」の一覧である。追加価値が費用に見合わない
- **共通書籍モデル（Block / Paragraph / Code …）** — 粗い索引に要るのは単位の境界・見出し・本文だけで、
  すべて既にある（読み順、目次、本文抽出）。構造化文書モデルは大工事であり、
  「木から書き戻さず、元の断片を切る」という本体の方針とも衝突する
- **SQLite 多表構成** — 本体は「1 つの JSON ＋ 書籍ごとの索引ファイル ＋ stamp で失効」で通してきた。
  SQLite FTS5 は理由を挙げて棄却済み（spec.md 第 10.4 節）。ベクトルも同じ形にする（第 4.3 節）
- **メタデータによる書棚（ISBN・出版社・出版日）** — 実測で 241 冊中 229 冊に著者すら無かった。
  ファイルに無いものは条件にできない。「Rust 関連」のような棚は、
  既存の横断検索の条件を保存する形（AI 不要）で先に作れる
- **モデル配布の仕組み** — OS が配るモデルから始める（第 4.6 節）。配布・署名・版管理は持たない
- **リーダー機能の拡張（縦書き・ハイライト・MathML・注釈・syntax highlighting）** —
  本編で決めた話の蒸し返しであり、この文書の主題と無関係。ここでは扱わない
- **図表の索引・OCR** — 別件とする。とくに OCR は「文字の層が無い PDF を検索可能にする」独立の価値があり、
  意味の層と混ぜずに検討する

### 1.4 契約の外であること

本体の背骨は「2 実装が同じ値を返すことを機械的に確かめる」である（conformance、81 件）。
二字組索引が契約に載ったのは「索引の有無で検索結果が変わらない」からである。

**意味の層はこれができない。** 埋め込み（embedding）が順位を決めるので、照合し直す「正解」が存在しない。
モデルの版が変われば結果も変わる。spec.md 第 13.3 節の言葉では
「入力から出力が一意に決まる部分」ではない。

したがって次を明示的に決める。

- 意味の層は**契約の対象外**とする。conformance には載せない
- **まず macOS 版に入れる。** Windows 版（Tauri）に足すかどうかは、価値が立ってから別に判断する。
  埋め込みの実装は各版で別になる（macOS は Swift + Core ML、Windows は kohagi）が、
  これは本体の形（同じ契約を各実装が独立に満たす）と同じである
- 品質は契約の代わりに**評価セット**で守る（第 8 章）

## 2. 設計原則

たたき台の原則を継承する。本体の既存の言葉と揃えて言い直す。

1. **Book First** — 中心は原書。AI は原書の必要な箇所へ届くための補助
2. **Local First** — 書籍本文を端末の外へ送らない。読書時ネットワーク不使用（spec.md 第 2.2 節）の不変条件を守る
3. **索引は地図** — 意味の索引も候補を出すだけで、最終的には原書から切り出した本文を見せる
4. **Search Before Generate** — 生成より検索・抽出を優先する。生成は最後の任意の段
5. **原文へ戻れる** — すべての結果に Locator を付け、原書の該当箇所へ飛べる
6. **Level 0 で完結** — 意味の層が無くても、いまのアプリがそのまま成立する

ただし本体の「索引は候補を絞るだけで、当たりを決めない」は、意味の層には適用**できない**。
意味の層には「当たり」という概念そのものが無い。
だから正確な検索と意味の検索は、結果の見せ方を分ける（第 5.2 節）。

## 3. 段階

| 段階 | 内容 | この文書での扱い |
| --- | --- | --- |
| Level 0 | いまのアプリそのまま（閲覧・書棚・二字組索引の検索） | 変えない |
| Level 1 | 意味の索引、関連箇所表示、意味検索 | **この文書の範囲** |
| Level 2 | 再順位付け（reranker）、重要文抽出 | **着手**（第 5.3 節） |
| Level 3 | 生成系（節概要・質問応答・「章を理解する」） | 保留。別文書で改めて起こす |

Level 3 について先に決めておくのは 2 点だけとする。
生成は検索・抽出の後の任意の段であること。原文に根拠が無ければ答えないこと。

## 4. 意味の索引

### 4.1 単位

**段落を単位とする。** 狙いは 400 字（およそ 200 トークン、技術書の 1〜2 段落）で、
文の切れ目で詰める。100 字未満の切れ端は独り立ちさせず、前に足す。

節ではなく段落にしたのは、使ってみて粗すぎたからである。

- **節を 1 本のベクトルにすると、3 つの話題に触れた節がそのどれでもない平均になる**
- 着いた先が節の頭で、当たった場所そのものは分からない
- 一覧に出る文も節の冒頭で、**なぜ当たったのかと無関係**だった

段落なら、飛び先がその段落になり、出す文がその段落そのものになる。

- EPUB：読み順の 1 項目を `h1`〜`h3` で割り、その中を段落に詰める。
  節の見出しは各段落に添える（どこの話かが分からないと見分けが付かない）
- PDF：**ページごとに切る**。節の範囲でまとめると着いた先が節の頭になる。
  ページ単位なら飛び先の頁が正確に決まり、目印で行まで寄せられる。
  節の見出しはアウトラインから引いて添える

**spikes/findings-unit-size.md の「割らない方がよい」はこの話ではない。**
あれは割った片を最大値で節にまとめ直し、**節を順位付けする**設定の話で、
片の多い節ほど得をするのが悪さの正体だった。片そのものを単位にすれば、
まとめ直しが無いのでその仕組みは働かない。一般化して書いたのは言い過ぎだった。

段落にして値段はむしろ下がった。段落は短いのでバケット 256 に収まり、
節のように 1024/2048 へ落ちない。実測（技術書 1 冊、252 段落）。

| | 節単位 | 段落単位 |
|---|---|---|
| 1 単位あたり | 中央 30〜90 ms | **中央 11.8 ms** |
| 1 冊 | 約 11 秒 | **6.4 秒** |
| 切り詰め | 13% | **0%** |
| sidecar | 約 0.1 MB／冊 | **0.35 MB／冊** |

増えるのは置き場所だけで、蔵書 241 冊なら 84 MB 見当である。

### 4.2 単位が持つもの

```text
単位（段落）
 ├─ locator（既存の位置表現。**本文の目印 30 字を含む**）
 ├─ その段落を含む節の見出し
 └─ ベクトル
```

**本文は控えない。出すときに原書から切り出す**（`PassageText`）。

一度は抜き書き 160 字を控えていたが、測ると**本の 35%（目印を入れて 42%）が
原文のまま sidecar に入っていた**。二字組索引は本文を持たず、候補を絞ってから
原書を走査し直す（spec.md 第 10.4 節）。意味の索引だけが写しを持つのは筋が通らないし、
置き場所を丸ごと写せば各書籍の 3 分の 1 が付いてくることになる。

控えるのをやめて **9.5%**、sidecar は 1 冊 0.35 MB → **0.27 MB**（うち文字 0.03 MB）。

- **目印（先頭 30 字）は本文の控えではなく、寄せるための位置である。**
  既存の読書位置や検索の印（`Locator.text`）と同じ性質のもの
- 見出しは目次と同じ性質の情報で、本文ではない
- 切り出しは**書籍ごとにまとめて 1 度開く**。1 件ずつ開くと 40 件で 40 回になる
- 画面は一拍おいて本文が入る。**索引から出すより遅いが、控えないための代償である**

**切り出す場所は位置で決め、目印は答え合わせに使う。**
EPUB は章の中の位置を 10 万分の 1 まで持つので、1,000 字の章なら 0.01 字の粗さになる。
目印だけで探すと**繰り返しの多い本文で手前の同じ並びに当たる**（検査で捕まえた）。
PDF は頁の中の位置を持たないので目印で探す。実測で 60 件すべて切り出せ、
うち 54 件が段落の頭ちょうどだった。

### 4.3 置き場所

書籍ごとの sidecar ファイルとする。二字組索引と同じ場所・同じ考え方。

- 失効の鍵は「ファイルの大きさ＋更新日時＋**モデルの版**」。sidecar の頭に 3 つとも置き、
  **中身をほどく前に頭だけで落とす**
- **探索は総当たりの内積で足りる**（Accelerate）。ANN は持たない
- **1 冊ずつ読んで、1 冊ずつ捨てる。** 先に全冊ぶんを集めてから並べてはいけない。
  ほどいた索引に上限を付けてあっても、集めた配列が握っていれば追い出されない
- **順位付けに要るものと、当たってから要るものを分けて置く**（形式の版 6）。
  並びは「節の番号 → ベクトル → メタデータ」。読み込みは前だけをほどき、
  飛び先・見出し・目印は**当たった書籍でしかほどかない**。
  一緒にほどくと、500 冊を引くたびに 29 万個の文字列を組み立てることになる
- **ベクトルは fp16 のまま持つ。** fp32 に広げて持つと同じ中身で倍の場所を食い、
  記憶に抱えられる冊数が半分になる。引くときに 1 冊ぶんずつ広げる（1 ms 未満）
- メタデータの文字列は**表で 1 度だけ**書く。見出しと章の道筋は同じ節の段落ぶん重複する
- **mmap は使わない。** 試したら冷えた読み込みが倍に伸びた（198 → 435 ms）。
  どうせベクトルは自前の並びへ写し取るので、頁例外を払うより順に 1 回で読む方が安い

段落単位での実測（1 冊 580 段落、512 次元、500 冊 29 万段落）。

| | 分ける前 | **分けた後** |
|---|---|---|
| 引く 冷（500 冊） | 198 ms | **107 ms** |
| 引く 温（500 冊） | 204 ms | **101 ms** |
| 足跡 | 173 MB | **84 MB** |
| sidecar 合計 | 302 MB | 297 MB |

置き場所は 1,000 冊で 600 MB 見当。二字組索引と同じく、書籍から何度でも作り直せる。
さらに半分にするなら int8 への量子化があるが、**順位への影響を評価セットで測ってから**とする。
ここまでの最適化は表現を変えていない（fp16 は元から）ので順位が変わらないが、int8 は変わり得る。

### 4.4 作るとき

- 二字組索引と同じ折で作る。**開いたとき**（割り込ませる）と、**書棚が落ち着いたとき**（8 秒待つ）。
  ただし**入にしていなければ何もしない**。開いた本以外は電源に繋がっているときだけ進む
- **初回が重いことを前提に設計する。** 段落単位なら 1 冊 6.4 秒（技術書 1 冊、252 段落の実測）。
  蔵書 241 冊で 26 分、1,000 冊なら 2 時間弱を見込む。
  優先順位（開いた本 → 最近読んだ本 → 残り）を付け、
  進み具合を見せ、途中でやめられるようにする（一括取り込みで学んだ形）
- 既定では電源接続時にだけ残りを進める
- **モデルの版が上がったら全量作り直しになる。それを黙ってやらない。**
  「まだ作っていない」と「作ったが版が変わった」を分けて数え、後者があればそう言う
  （前者は待てば済むが、後者は作り直すまでその書籍が結果に出ない）。
  数えるのは sidecar の**頭だけ**なので、蔵書ぶん呼んでも安い

### 4.5 メモリ

**握り続ける値段は、思っていたより安い。** 実測でこうなった。

| 置いた時間 | 足跡 | 実 RAM |
|---|---|---|
| 推論の直後 | 112 MB | 140 MB |
| 15 秒 | 112 MB | 25 MB |
| 3 分 | **112 MB** | **17 MB** |

**数百 MB は推論中の一時的な作業領域**で、数秒で返る。落ち着いてからは
写した重みのページが RAM から追い出され、**置くほど減る**。膨らまない。

一方、手放して作り直すと**語彙 127 ms ＋ バケット 456 ms ＝ 583 ms** かかる。
置いたままページが追い出されていても、引き直しは 25〜30 ms で済む。
**3.7 ms と 25 ms の差は人に分からないが、25 ms と 583 ms の差は分かる。**

したがって温かさを保とうとはせず、**握っておくことだけを考える**（`EmbedderHolder`）。

- **一度作ったら持ち続ける。** 時計で降ろすのはやめた。膨らまないものを見張る値打ちが無い
- **降ろすのは意味の層を切ったときだけ。** 使う見込みが無くなる唯一の折である
- 「本を全部閉じたら降ろす」も試したが外した。
  **書棚こそ意味検索を打つ場所**で、本を閉じた後に引くと 583 ms 払うことになる
- 使っている最中は降ろさない（索引作りは裏で走る）

### 4.6 モデルと埋め込みの経路

**OS が配るモデルでは届かない。** NLContextualEmbedding は文の埋め込みではなく、
長さが話題を上回るため順位が立たない（スパイクで実測）。日本語の文埋め込みは
OS から得られない（`NLEmbedding.sentenceEmbedding` は日本語未対応）。

- **Ruri v3 の 130m**（日本語の文埋め込み、512 次元）を既定とする。
  検索用の接頭辞を持ち、文書側は `検索文書: `、問い側は `検索クエリ: ` を付ける。
  関連箇所（文書どうしの比較）は両方 `検索文書: ` で揃える
- **30m は選べるが既定にしない**（spikes/findings-model-size.md で実測）。
  78 MB・3 倍速いが、合格線にちょうど乗るだけで余裕が無く、外し方が語の重なりに寄る。
  語の一致で寄る仕事は bigram の索引の方が速くて正確なので、
  **安くなった分だけ、意味の層に足したかった価値が減っている**。
  埋め込みは書籍を入れたときの 1 度きりで裏に隠れるため、速さの得は効く場所に効かない。
  差し替えは次元も平均の取り方もモデル自身の申告から読んでいるので、置き場所を変えるだけで通る
  （実装を変えずに読めることを確かめてある）
- **意味の層は macOS 15 以降とする。** 変換物は長さごとの固定形を 1 つの束にまとめた
  multi-function 形式で、これを選ぶ `MLModelConfiguration.functionName` が macOS 15 以降にしかない。
  個別形にすれば 14 でも動くが、264 MB が 794 MB になる。
  段階制（第 3 章）なので、14 では Level 0 のまま成立する
- **実装は Swift で完結させる。** Core ML を直に呼び、トークナイザも Swift で持つ。
  macOS 版は自己完結という本体の構成（spec.md 第 13.1 節）を崩さない。
  Core AI / Foundation Models も Swift からしか呼べないため、
  Level 3 へ進んだときに AI 層が二つの形に割れない
- **トークナイザは黙って間違う。** 食い違っても例外は出ず、違うベクトルが出るだけである。
  参照実装（kohagi、Rust の tokenizers crate）との**トークン ID 一致を検査で固める**
  （spikes/findings-swift-tokenizer.md）。Swift の String は正準等価で比べるため、
  語彙の鍵も空白の置き換えもバイト列で行う。点の足し込みは Double で行う
- モデルは公開済みの Core ML 変換物を使う。変換は kohagi（`coreml-convert`）に残す。
  取得は意味の層を有効にした時点の 1 度だけで、読書時ではない。**書籍本文は送らない**
- **バケットは使うときに開く。** 同じ束の中では開く値段は長さに依らず、
  1 回の埋め込みが使うのは 1 本だけである。全部を先に開くと 4.1 秒待たされるが、
  要ったときに開けば読み込みは **135 ms** で済む（spikes/findings-buckets.md）。
  値段は初回の 1 回だけそのバケットへ移り、問いを引くだけなら短い 1 本で足りる
- **開く値段は束の大きさに比例する。** 本数ではなく `model.mlmodel` の大きさで決まり、
  `30 ms + 71 ms × MB` に乗る（1 本開くのに束のグラフ全体を読んでいる）。
  短いバケットはほぼタダで、値段は長いバケットが払う（2048 だけで 3.5 MB ほど）。
  **3 本（64,512,2048）の方が 4 本（64,128,256,512）より遅い**のがその証拠である
- **`64,128,256,512,1024,2048` の 6 本とする。** 768 と 1536 を足すと索引作りが 1 割強速くなるが、
  代わりに 1 本開く値段が 443 ms → 655 ms になり、開くたびに払い続ける。割に合わない。
  蔵書が桁違いに増えて索引作りが問題になったら、そのとき足せばよい
  （**置き場所を差し替えるだけで実装は変わらない**）
- **短いバケットは裏で先に開いておく。** 裏で走る索引作りに 443 ms は見えないが、
  **launch 後の最初の問いだけは表に出る**。意味の層を入にした時点で先に開いておけば待ち時間は 0 になる
- **公開物を 6 本で作り直す必要がある。** いま置いてある `takahashim/ruri-v3-130m-coreml` は
  `64,128,256,512` である。配る大きさは 253.4 MB → 260.2 MB。
  **同じ名前で差し替えるか別名にするかは先送りとする。** 6 本は 4 本の上位互換で
  （512 以下では同じバケットを選ぶので同じベクトルを返す）、chororeader としては
  作り分ける理由が無い。ただし公開先を他からも使っているなら、
  黙って差し替えると短い文しか埋め込まない側の開く時間が 4 倍になる。そこは公開先の事情で決める
- **アプリはモデルの出どころを知らない。** 決まった場所を見るだけにしておく
  （`EmbeddingModelStore`）。配り方が決まったら、そこへ書く手順が足されるだけで使う側は変わらない。

  ```text
  ~/Library/Application Support/ChoroReader/Models/ruri-v3-130m-coreml/
  ```

  **Caches には置かない。** OS はいつでも消してよいことになっており、消えると 260 MB を取り直す。
  組み直したもの（`.mlmodelc`）は 7 秒で作れるので、そちらは Caches のままでよい
- **使うときに開く形は、共有され書き換わる状態を持つ。** 索引作り（裏）と問い（前）が
  同じ埋め込み器を叩くので閂を掛ける。この競合は検査でも Thread Sanitizer でも捕まえられなかった。
  窓が狭いためで、無いわけではない
- 配る形（`.mlpackage`）はそのままでは読めない。**1 度組み直して置いておく**
  （1 束 20 秒。`~/Library/Caches/ChoroReader/CoreML`）。
  置き場所が符号の連なりで組まれていることがあるので、解いてから組む
- 多言語の小型モデル（bekko a25m）は速く索引も小さいが、意味検索が線に届かなかった。
  知識に依存する検索（技術書の横断はこれに当たる）で差が出る
- モデルの抽象（Embedder）を 1 枚だけ置く。検査で決定的な偽物を差すため（第 8 章）。
  それ以上の抽象（Reranker、Analyzer …）は Level 2 以降で必要になってから作る

### 4.7 kohagi の役割

埋め込みを Swift で持つとしても、kohagi（Rust + Candle）は 3 つの役目で残る。

1. **変換器** — `coreml-convert` が Core ML のバンドルを作る。公開手順も向こうにある
2. **照合の参照** — Swift 実装のトークン ID とベクトルを突き合わせる相手。
   期待値は 1 度作って凍結する
3. **Windows 版の実装** — Tauri 側に意味の層を入れるなら、kohagi が素直（CUDA にも対応する）

## 5. 意味検索

### 5.1 流れ

```text
問い
 ↓ 埋め込み
節で順位を付ける（段落のベクトルの平均）
 ↓ 節の中でいちばん近い段落を選ぶ
 ↓ 原書から本文を切り出す
原文断片 ＋ Locator
```

**順位は節で決め、着地は段落でする。**

段落だけで順位を付けると話題の芯を失う。400 字の段落は「学習」に触れていれば
「過学習を防ぐ方法」に高く出るが、本当に読みたい「正則化」の節には届かない。
巻末（解答・問題・索引）も段落に割れるぶん湧きやすくなる。

| 順位の付け方 | 意味検索 |
|---|---|
| 段落だけで順位 | 8/15 |
| 節単位（段落に割る前） | 10/15 |
| **節で順位 → 段落へ着地** | **11/15** |

節のベクトルは**段落のベクトルの平均**で足りるので、別に持たなくてよい。
節の点は平均で決める。最大にすると段落の多い節ほど得をする
（節単位のスパイクで測った悪さと同じ）。

切り出しは既存の本文抽出をそのまま使う。

### 5.3 再順位付け（Level 2）

**効くことはスパイクで確かめた**（spikes/findings-reranker.md）。上位 40 件を
cross-encoder で並べ直すと、上位 3 件に使えるものが 7〜9/15 → 10〜12/15 に上がる。
ベクトルの近さは話題しか見ないが、reranker は本文を読むので、
**話題は合うが中身が違う**候補を落とせる。

- モデルの第一候補は **japanese-reranker-xsmall-v2**（MIT、10 層・256 次元）。
  ruri-v3-30m と同じ形なので ANE の値段が実測から見積もれる：40 件で 160 ms 前後。
  310m は質で上だが 1 問あたり秒級になり、表示のたびには向かない
- **前提の壁**：kohagi の変換器は分類頭を弾く。出口を 1 つ足す必要がある（別リポジトリ）
- **巻末の雑音は reranker では直らない。** 日本語 reranker はクイズ形式の傾向を
  学んでいて、解答・演習の紙面を正解の説明と取り違える。悪化する問いすらある。別件で扱う
- 照合の検査は埋め込みと同じ流儀で固める（参照実装で期待値を凍結、架空の例文のみ）

### 5.2 見せ方

**正確な検索と混ぜない。** 二字組索引の検索は「当たり」を数えられるが、意味検索の結果は当たりではない。

- 件数を出さない（「17 件」と言えるのは正確な検索だけ）
- 書棚の検索に「似た内容」の段として分けて出す
- 出すのは代表文ではなく、原書から切り出した本文断片。開けばその場所へ飛ぶ（既存の導線）

## 6. 関連箇所表示

読書の窓のサイドバーに 1 区画を足す（新しい窓や覆いは作らない）。

```text
選んだ本文
 ↓
他の書籍の近い段落
 ↓
原書から本文を確かめて提示
```

**勝手に出さない。選んだところを種にする。**

読み進めるたびに引き直す形で一度作ったが、使ってみて立たなかった。
常時出ている情報は、当たらなければ視界の邪魔にしかならない。
しかも「似ている」としか言えないので、渡る理由が弱い。
「ここに近いものを知りたい」と思った時にだけ引く方が、外れも許容される。

- 呼ぶのは 3 つの道から。サイドバーの釦、メニュー（⇧⌘R）、右クリック
- **何を種にしたかを画面に出す。** 選び直したのに前の結果を見ていた、を防ぐ
- 種は本文どうしの比較なので、**両側とも `検索文書: ` で揃える**（第 4.6 節）
- 出すのは書名・節の見出し・当たった段落の頭。押せばその箇所を開く
- いま読んでいる本は出さない
- **選んだ本文は端末の外へ出ない**（第 2 章の 2）

なお当初この機能を「本命」と書いたが、使ってみると
問いを持っている側（意味検索）の方が立つ。重み付けを取り違えていた。

## 7. 権利

- 書籍本文・埋め込み・解析結果を端末の外へ送らない
- **本文を控えない。** 持つのは飛び先（本文の目印 30 字を含む）・節の見出し・ベクトルまでで、
  一覧に出す本文は原書から切り出す（第 4.2 節）。控えていた頃は本の 35% が原文のまま入っていた
- 埋め込みも目印も**原書を探すための派生データ**であり、原書の代替物を作らない
- 解析結果を自動で公開する仕組みを持たない

## 8. 検証

conformance の代わりに、次の 2 段で守る。

1. **配管の検査** — Embedder に決定的な偽物を差し、単位の切り出し・sidecar の失効・
   候補から原文への解決・Locator の往復を通常の検査で固める。ここは決定的にできる
2. **品質の評価セット** — 手元の蔵書に対する数十問（関連箇所の判定と意味検索の問い）を 1 度人手で採点し、
   モデルや粗さを変えたら流し直す。スパイクで作ったものを引き継ぐ

評価セットは実在の書籍の題名・著者・本文を含むため、**git には入れない**。
リポジトリに書き残すのは数字と抽象的な記述だけとする。

## 9. たたき台から引き継ぐ判断

- MVP に生成 AI を入れない（たたき台 §42 と同じ）
- 段階制（Level 0–3）を保つ。下の段は上の段が無くても完結する
- 「原文に根拠が無ければ答えない」を生成系の前提として先に固定しておく
