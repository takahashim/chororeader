<Window x:Class="ChoroReader.App.ReaderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="ChoroReader" Height="800" Width="700">
    <!--
        殻はネイティブ、本文だけ WebView2。
        道具帯・目次・検索はこの殻へ足していく。WebView には入れない。
    -->
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="ChapterLabel" Text="" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="PositionLabel" Text="" />
            </StatusBarItem>
        </StatusBar>
        <wv2:WebView2 x:Name="Body" />
    </DockPanel>
</Window>
