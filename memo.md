# macOS向け日本語技術書EPUBリーダー 仕様案

## 1. 概要

### 1.1 仮称

**Tech EPUB Reader for macOS**

### 1.2 目的

日本語の技術書をmacOS上で快適に読むための、軽量かつ高速なEPUBリーダーを開発する。

対象となる書籍は、主に次の2種類とする。

* HTMLとCSSで構成されたリフロー型EPUB
* 各ページが画像または固定配置HTMLで構成された固定レイアウトEPUB

一般的な小説リーダーではなく、以下の技術書特有の閲覧行動を重視する。

* 目次を頻繁に往復する
* 本文とコード例を参照する
* 図、表、スクリーンショットを拡大する
* 用語や関数名を検索する
* 複数箇所を行き来する
* ハイライトやメモを残す
* URLやコードをコピーする
* 読みながらターミナルやエディタを操作する
* 複数の書籍を並行して参照する

## 2. 基本方針

### 2.1 製品方針

* macOSネイティブアプリとして実装する
* SwiftUIをアプリUIの中心にする
* EPUB処理にはReadium Swift Toolkitを使用する
* 本文表示にはReadiumの`EPUBNavigatorViewController`を使用する
* 初期版ではReadiumを大きく改変しない
* 計測結果に基づき、Navigator周辺を段階的に最適化する
* 書籍ファイルを外部サービスへ送信しない
* ローカルファイルを中心とした単純な蔵書管理にする
* EPUBの構造を過度に正規化せず、原本を保持する

Readium Swift Toolkitはリフロー型EPUBと固定レイアウトEPUBの両方をサポートし、本文抽出、検索、ハイライトなどに利用できるAPIを持つ。

### 2.2 優先順位

優先順位は次の順とする。

1. 読書開始までの速さ
2. ページ送りとスクロールの滑らかさ
3. コード、図表、画像の視認性
4. 目次・履歴・検索による移動のしやすさ
5. コピー、ハイライト、メモ
6. 日本語組版の品質
7. 蔵書管理
8. 高度な読書支援機能

### 2.3 初期段階で対象外とするもの

* 電子書籍ストア
* 書籍販売
* 独自DRM
* SNS機能
* WindowsおよびLinux対応
* iPhoneおよびiPadとの同期
* PDFリーダー
* MOBI、AZW、Kindle形式
* EPUB編集
* 書籍本文の自動改変
* クラウドへの書籍アップロード

Readium LCPについては、必要性が確認された段階で追加できる構造にはしておくが、MVPには含めない。

## 3. 対象EPUB

## 3.1 リフロー型EPUB

主に次の要素を含む日本語技術書を対象とする。

* 横書き本文
* 見出し
* 段落
* 箇条書き
* コードブロック
* インラインコード
* 表
* 図版
* 数式
* 脚注
* 外部リンク
* 章間リンク
* 索引
* ルビ
* 縦中横
* SVG
* Webフォント

縦書き技術書も可能な範囲で表示するが、初期版では横書きを主対象とする。

### 3.2 画像中心の固定レイアウトEPUB

以下の形式を対象とする。

* spineの各項目がページ画像になっているEPUB
* XHTML上にページ画像を全面配置したEPUB
* SVGでページを構成したEPUB
* 固定座標で文字と画像を配置した固定レイアウトEPUB
* 見開き指定を持つEPUB

Readiumの固定レイアウトNavigatorを利用し、ページの単独表示、見開き表示、フィット方法を制御する。現行Readium Swiftでは固定レイアウトEPUBの表示、見開き、ページフィットなどが継続的に改善されている。

### 3.3 形式判定

書籍を開く際、次の表示種別へ分類する。

```text
Publication
├── Reflowable EPUB
├── Fixed-layout EPUB
│   ├── Bitmap page
│   ├── SVG page
│   └── XHTML fixed layout
└── Unsupported or malformed EPUB
```

判定結果は書籍データベースへ保存し、次回以降は再判定しない。

## 4. 画面構成

## 4.1 メインウィンドウ

基本構成は3ペインとする。

```text
┌─────────────────────────────────────────────────────┐
│ ツールバー                                          │
├──────────────┬──────────────────────────┬───────────┤
│ ナビゲータ   │ 本文                     │ 情報      │
│              │                          │ パネル    │
│ ・目次       │                          │           │
│ ・検索       │                          │ ・注釈    │
│ ・履歴       │                          │ ・メモ    │
│ ・しおり     │                          │ ・図一覧  │
└──────────────┴──────────────────────────┴───────────┘
```

左ペインと右ペインは独立して表示・非表示を切り替えられる。

### 4.2 ツールバー

以下の操作を配置する。

* サイドバー表示切り替え
* 戻る
* 進む
* 目次
* 検索
* しおり追加
* 表示設定
* ページ／位置表示
* 拡大・縮小
* 情報パネル表示切り替え

ツールバー項目はmacOSのツールバーカスタマイズに対応する。

### 4.3 ウィンドウ管理

* 複数書籍を別ウィンドウで開ける
* 同一書籍を複数ウィンドウで開ける
* 前回開いていたウィンドウを復元する
* ウィンドウごとに表示位置を保持する
* タブによる複数書籍表示を許可する
* macOSのSplit Viewに対応する
* フルスクリーン表示に対応する

同一書籍の複数ウィンドウは、技術書の本文と付録、説明とコード一覧などを並べる用途を想定する。

## 5. 本棚とファイル管理

## 5.1 書籍の追加

以下の方法を提供する。

* FinderからEPUBを開く
* Dockアイコンへドロップする
* アプリウィンドウへドロップする
* 「ファイルを開く」から選択する
* フォルダをライブラリとして登録する

### 5.2 ファイルの保持方法

既定では、元のEPUBファイルをアプリ領域へ複製しない。

```text
ユーザーのEPUB
    ↓ Security-Scoped Bookmark
アプリ内データベース
    ├── ファイル参照
    ├── メタデータ
    ├── 表紙キャッシュ
    ├── 検索インデックス
    └── 注釈
```

オプションとして「ライブラリへコピー」を用意してもよい。

### 5.3 ファイル変更の検出

次の値を保存する。

* ファイルURL
* ファイルサイズ
* 更新日時
* 必要に応じてハッシュ値

元ファイルが変更された場合は、次を再生成する。

* Publicationキャッシュ
* 表紙
* 目次
* 検索インデックス
* 位置情報

既存の注釈は可能な範囲で再対応付けする。

## 6. リフロー型EPUBの閲覧

## 6.1 表示方式

次の2方式を提供する。

* ページ送り
* 縦スクロール

初期設定は縦スクロールとする。

技術書では、コードや表がページ境界で分断されにくく、スクロール位置も把握しやすいためである。

### 6.2 ページ送り

* 左右キーで前後ページへ移動する
* `Space`で次ページへ移動する
* `Shift + Space`で前ページへ移動する
* トラックパッドの水平スワイプに対応する
* ページ末尾から次のspine項目へ連続して移動する
* ページ送り中のアニメーションは短くするか無効化できる

### 6.3 スクロール

* トラックパッドの慣性スクロールに対応する
* spine境界を越えて連続的に読めるように見せる
* 実装上は現在章と隣接章のみをロードする
* 巨大な章では単一WebView内で表示する
* スクロール位置通知はthrottleする
* スクロール中は不要なページ数再計算を行わない

### 6.4 表示設定

以下を変更できる。

* 文字サイズ
* 行間
* 本文幅
* ページ余白
* 書体
* 背景色
* 文字色
* リンク色
* コード用書体
* コード用文字サイズ
* ページ送り／スクロール
* 出版社スタイル優先／ユーザー設定優先

表示設定は即時反映するが、再レイアウトが必要な設定は100〜200ミリ秒程度debounceする。

### 6.5 本文幅

技術書向けに、本文幅の上限を設定できる。

候補：

* 狭い
* 標準
* 広い
* ウィンドウ幅いっぱい
* カスタム

本文をウィンドウいっぱいに引き伸ばすのではなく、中央に一定幅で配置できるようにする。

### 6.6 出版社CSSの扱い

次の3段階を提供する。

1. 出版社スタイルを優先
2. 読みやすさに関する設定だけ上書き
3. アプリ標準スタイルを優先

ただし、次の要素は不用意に破壊しない。

* コードブロック
* 表
* 数式
* 図版
* 脚注
* 複雑なレイアウト
* 固定幅を前提とした要素

## 7. コード表示

## 7.1 コードブロック

コードブロックは技術書の最重要要素として扱う。

* 等幅フォントを使用する
* タブ幅を設定できる
* 行折り返しを切り替えられる
* 横スクロールに対応する
* コードブロック単位でコピーできる
* コピー時に不要な行番号を除外する
* 全角空白やタブを可視化できる
* 長い行の末尾を確認しやすくする
* コードブロックを別ウィンドウで拡大表示できる

### 7.2 コードブロック操作

コードブロックへマウスを置いた際、右上に小さな操作ボタンを表示する。

* コピー
* 拡大
* 行折り返し切り替え

本文選択を妨げないよう、通常時は目立たせない。

### 7.3 シンタックスハイライト

出版社側で色付けされている場合は原則として維持する。

アプリ側の自動シンタックスハイライトはMVPでは行わない。

将来的には、次の条件を満たす場合に限定して追加する。

* `class="language-ruby"`など言語情報がある
* コードがプレーンテキストである
* 出版社側の装飾が存在しない

## 8. 表の表示

* 横幅を超える表は横スクロール可能にする
* 表だけを拡大表示できる
* 表のセルを選択・コピーできる
* 表全体をTSVとしてコピーできる拡張余地を持つ
* 表を画像として書き出す機能は将来対応とする
* 表内の固定幅指定を必要に応じて緩和する
* 表のヘッダーを拡大表示時に固定できるようにする

小さなウィンドウで表を無理に縮小せず、専用の拡大ビューを用意する。

## 9. 図版と画像

## 9.1 本文内画像

* クリックまたはダブルクリックで拡大する
* ピンチ操作で拡大・縮小する
* 原寸表示に切り替えられる
* ウィンドウに合わせて表示できる
* 背景色を白、黒、透明市松から選べる
* 画像をFinderへドラッグできる
* 画像をコピーできる
* 代替テキストを確認できる

### 9.2 画像ビューア

画像は本文上で直接拡大するのではなく、独立したオーバーレイまたはパネルで表示する。

```text
本文
  ↓ 画像を開く
画像ビューア
  ├── 拡大縮小
  ├── 100%表示
  ├── ウィンドウに合わせる
  ├── コピー
  ├── 保存
  └── 前後の図へ移動
```

### 9.3 図一覧

書籍内の画像を一覧表示する「図一覧」を将来追加できる構造にする。

一覧には以下を表示する。

* サムネイル
* キャプション
* 章
* 位置
* 代替テキスト

## 10. 固定レイアウト／画像型EPUBの閲覧

## 10.1 表示モード

以下を提供する。

* 単ページ
* 見開き
* 連続縦スクロール
* サムネイル一覧

初期設定は単ページとする。

### 10.2 フィット

* ページ全体
* 横幅に合わせる
* 高さに合わせる
* 原寸
* 前回の倍率を維持

固定レイアウトでは文字が画像化されている場合があるため、拡大操作をすぐ使えることを重視する。

### 10.3 拡大操作

* `⌘ +`で拡大
* `⌘ -`で縮小
* `⌘ 0`でページ全体
* ピンチ操作
* ダブルクリックで指定位置を拡大
* 拡大時にドラッグで移動
* 拡大状態のまま次ページへ移動可能

### 10.4 見開き

* 表紙は単独表示
* 左開き／右開きを出版物メタデータから判断する
* ユーザーが見開き方向を上書きできる
* ウィンドウ幅が不足する場合は自動的に単ページへ切り替える
* 自動切り替えを無効化できる

### 10.5 画像型EPUBの検索

本文が画像だけの場合、通常の全文検索は利用できない。

検索機能は次の状態を明示する。

* テキスト検索可能
* 一部テキスト検索可能
* 画像のみで検索不可
* OCRインデックスあり

OCRはMVPには含めない。

将来、Vision Frameworkを使い、ローカルで日本語OCRインデックスを生成できるようにする。ただしOCR結果は原文ではなく検索補助データとして扱う。

## 11. ナビゲーション

## 11.1 目次

* 階層目次をアウトライン表示する
* 現在位置に対応する項目を選択状態にする
* キーボードで展開・折りたたみできる
* 目次項目を別ウィンドウで開ける
* 目次項目を右クリックして新しいタブで開ける
* 目次内を絞り込み検索できる

### 11.2 履歴

Webブラウザに近い戻る・進む履歴を持つ。

履歴対象：

* 目次による移動
* 本文内リンク
* 検索結果
* 脚注
* 索引
* しおり
* 注釈
* ページ番号入力
* 外部から指定されたLocator

単純な前ページではなく、「参照前の場所へ戻る」操作を提供する。

### 11.3 脚注

脚注リンクをクリックした場合は、可能ならポップオーバーで内容を表示する。

* 本文位置を失わない
* ポップオーバー内のリンクも操作できる
* 脚注全体へ移動する操作も用意する
* 閉じると元の位置へ戻る

### 11.4 章間参照

「第3章を参照」などの内部リンクは、通常のリンク移動として履歴へ積む。

修飾キー付きクリックでは、別タブまたは別ウィンドウで開ける。

## 12. 検索

## 12.1 検索対象

* 本文
* 見出し
* コード
* 図の代替テキスト
* キャプション
* 脚注
* 索引語
* ユーザーのメモ

### 12.2 検索モード

* 部分一致
* 大文字・小文字を区別
* 単語単位
* 正規表現
* コード優先
* 見出し優先

日本語では単語境界が明確でないため、標準では部分一致を用いる。

### 12.3 検索結果

検索結果には以下を表示する。

* 章名
* 前後の文脈
* マッチ文字列
* 本文／見出し／コードなどの種別
* 書籍内の概算位置

コード内の結果は等幅フォントで表示する。

### 12.4 検索インデックス

初回表示のクリティカルパスでは作成しない。

```text
書籍を開く
  ↓
現在位置を表示
  ↓
アイドル時に本文抽出
  ↓
SQLite検索インデックス作成
```

インデックス作成中も、ReadiumのPublication Search APIによる簡易検索へフォールバックできる構成とする。ReadiumはPublication内容の抽出および検索用APIを提供している。

日本語検索では、初期版は文字列部分一致または文字N-gramを使用する。

## 13. テキスト選択とコピー

## 13.1 選択

* マウスによる通常選択
* `Shift`を使った範囲拡張
* 段落単位選択
* コードブロック単位選択
* 複数範囲選択は将来対応

### 13.2 コピー

コピー形式として次を提供する。

* プレーンテキスト
* Markdown
* 引用情報付きテキスト
* HTML
* コードのみ

引用情報付きの場合は次を付加する。

```text
引用本文

『書名』著者名、第3章「章タイトル」
```

ページ番号がEPUB内に存在する場合はページ番号も付ける。

### 13.3 URL

本文中のURLは次の操作に対応する。

* 既定ブラウザで開く
* URLをコピー
* リンク文字列をコピー
* リンク先を確認する

外部URLを開く前に、URLをステータス表示する。

## 14. ハイライト、しおり、メモ

## 14.1 ハイライト

* 選択範囲へハイライトを付ける
* 複数の種類を設定できる
* 下線表示も選択できる
* 現在のspine項目だけDOMへ適用する
* 同一グループのDecorationをまとめて反映する

ReadiumのDecoration APIは`EPUBNavigatorViewController`で利用でき、ハイライトや下線を実装できる。

### 14.2 ハイライト種別

色だけでなく意味を付ける。

* 重要
* 後で試す
* 不明点
* 誤植候補
* 参照
* 任意タグ

### 14.3 メモ

* ハイライトにメモを付ける
* 位置だけにメモを付ける
* Markdownで記述できる
* 書籍内リンクをメモへ挿入できる
* メモ一覧から本文へ移動できる

### 14.4 しおり

しおりは選択範囲ではなく読書位置へ付与する。

* 名前を変更できる
* タグを付けられる
* 章ごとに一覧表示する
* キーボードショートカットで追加する

## 15. 読書位置

## 15.1 保存情報

ReadiumのLocatorを基礎として、以下を保存する。

* `href`
* `type`
* `locations.progression`
* `locations.totalProgression`
* CSS selector
* テキスト位置情報
* 章タイトル
* 表示モード
* スクロール位置
* 更新日時

### 15.2 復元

復元は次の順で試行する。

1. CSS selectorまたはfragment
2. テキスト位置情報
3. resource内progression
4. 書籍全体のprogression
5. spine項目の先頭

固定レイアウトではページ番号と拡大率も保存する。

## 16. キーボード操作

主要操作にはキーボードショートカットを割り当てる。

| 操作       | ショートカット案            |
| -------- | ------------------- |
| EPUBを開く  | `⌘O`                |
| 検索       | `⌘F`                |
| 書籍全体を検索  | `⌘⇧F`               |
| 次ページ     | `→` / `Space`       |
| 前ページ     | `←` / `Shift Space` |
| 次章       | `⌘↓`                |
| 前章       | `⌘↑`                |
| 戻る       | `⌘[`                |
| 進む       | `⌘]`                |
| しおり      | `⌘D`                |
| 目次表示     | `⌘⇧T`               |
| 文字／ページ拡大 | `⌘+`                |
| 文字／ページ縮小 | `⌘-`                |
| 標準倍率     | `⌘0`                |
| サイドバー    | `⌘⌥S`               |
| 情報パネル    | `⌘⌥I`               |

ページ型とリフロー型で意味が異なる操作は、現在の表示形式に応じて切り替える。

## 17. macOS連携

* Quick Lookプラグインは将来対応
* Spotlight検索への書誌情報登録
* Finderから「このアプリで開く」
* 最近使った項目への対応
* Servicesによる選択テキスト受け渡し
* Shareメニューによる引用共有
* AppleScriptまたはShortcuts対応の拡張余地
* Universal Linksまたは独自URL Schemeによる書籍位置指定

独自URLの例：

```text
techepub://book/<book-id>?href=chapter03.xhtml&progression=0.42
```

これにより、メモアプリやIssue、ソースコードコメントから書籍位置を参照できる。

## 18. アーキテクチャ

## 18.1 全体構成

```text
SwiftUI App
├── Library
│   ├── BookRepository
│   ├── FileAccess
│   └── CoverCache
├── Reader
│   ├── ReaderSession
│   ├── ReaderWindow
│   ├── NavigationHistory
│   └── ReadingPositionStore
├── Readium
│   ├── AssetRetriever
│   ├── PublicationOpener
│   ├── Publication
│   └── EPUBNavigatorViewController
├── Search
│   ├── ContentExtractor
│   ├── SearchIndexer
│   └── SearchRepository
├── Annotation
│   ├── HighlightRepository
│   ├── BookmarkRepository
│   └── NoteRepository
└── Persistence
    └── SQLite
```

Readiumの`ReadiumStreamer`はEPUBなどのファイルを`Publication`へ解析し、NavigatorがそのPublicationを表示する構成になっている。

### 18.2 ReaderSession

1つの読書ウィンドウにつき1つの`ReaderSession`を持つ。

```swift
@MainActor
final class ReaderSession: ObservableObject {
    let bookID: BookID
    let publication: Publication
    let navigator: EPUBNavigatorViewController

    @Published var location: Locator?
    @Published var navigationHistory: NavigationHistory
    @Published var displayMode: ReaderDisplayMode
}
```

SwiftUI Viewの再生成によってNavigatorやPublicationが作り直されないようにする。

### 18.3 Readium統合

初期版では次のモジュールを利用する。

* ReadiumShared
* ReadiumStreamer
* ReadiumNavigator

LCPとOPDSは必要になった時点で追加する。

現行Readium Swiftでは、EPUBリソースはNavigatorのカスタムURL Scheme HandlerからWebViewへ提供され、以前必要だったローカルHTTPサーバーは不要になっている。

## 19. データモデル

### 19.1 Book

```text
Book
├── id
├── fileBookmark
├── fileSize
├── fileModificationDate
├── contentHash
├── mediaType
├── layout
├── title
├── authors
├── language
├── identifier
├── coverCacheKey
├── lastOpenedAt
├── lastLocator
└── indexingStatus
```

### 19.2 Annotation

```text
Annotation
├── id
├── bookID
├── kind
├── locatorJSON
├── selectedText
├── note
├── category
├── createdAt
└── updatedAt
```

### 19.3 NavigationHistoryEntry

```text
NavigationHistoryEntry
├── bookID
├── locatorJSON
├── title
├── reason
└── createdAt
```

`reason`には次を保存する。

* manual
* toc
* search
* internalLink
* footnote
* bookmark
* annotation

## 20. 性能要件

## 20.1 目標環境

* Apple Silicon Mac
* ローカルストレージ上のEPUB
* 数MBから数百MB程度の技術書
* 数百のspine項目
* 高解像度画像を多数含む書籍

### 20.2 体感性能目標

目標値はプロトタイプ計測後に調整する。

* アプリのウィンドウ表示：即時
* 登録済み書籍の本文表示：原則1秒以内
* 同一章内のページ送り：100ミリ秒以内に応答開始
* 章移動：300ミリ秒以内に応答開始
* 検索結果の初回表示：200ミリ秒以内
* 目次表示：即時
* 画像拡大ビューの表示：200ミリ秒以内
* スクロール：通常時60fpsを目標
* UI操作中にメインスレッドを100ミリ秒以上連続占有しない

これらは保証値ではなく、最適化判断のための設計目標とする。

### 20.3 読書開始のクリティカルパス

```text
ファイル参照の復元
    ↓
Publicationを開く
    ↓
前回Locatorを読み込む
    ↓
Navigatorを表示する
```

以下は本文表示後へ遅延する。

* 検索インデックス生成
* 全文抽出
* 全位置情報生成
* 図一覧生成
* サムネイル生成
* 書籍全体の注釈解決
* OCR
* アクセシビリティ情報の詳細解析

### 20.4 画像キャッシュ

固定レイアウトおよび図版では次を行う。

* 原画像を無条件に全展開しない
* 表示ページと前後ページを先読みする
* サムネイルは別サイズで生成する
* メモリ警告時に画像キャッシュを破棄する
* ウィンドウサイズに対して過剰な解像度でデコードしない
* 原寸表示時のみ高解像度画像を読み込む

### 20.5 計測

`os_signpost`を使用し、少なくとも次を計測する。

* EPUB取得
* Publication解析
* Navigator生成
* 最初のresourceロード
* DOM ready
* 初回レイアウト完了
* Locator復元
* Decoration適用
* 章移動
* 画像デコード
* 検索インデックス生成

## 21. エラー処理

以下を区別して表示する。

* EPUBではない
* ZIPが破損している
* OPFを解析できない
* spineが空
* 参照リソースが欠落している
* 暗号化されている
* DRMで保護されている
* 対応していない固定レイアウト
* ファイルへのアクセス権が失われた
* ファイルが移動または削除された

可能な場合は、一部が壊れたEPUBでも読める章を表示する。

詳細エラーはログへ記録し、ユーザーには簡潔な説明と診断情報のコピー操作を提供する。

## 22. セキュリティ

* EPUB内のJavaScriptは既定で無効化する
* 外部ネットワークアクセスを既定で禁止する
* 外部URLはOSのブラウザで開く
* EPUB内リソースからローカルファイルへアクセスさせない
* カスタムURL Schemeの対象をPublication内リソースに限定する
* 不正なパスを正規化して検証する
* 巨大画像、ZIP bomb、過度な展開サイズを検出する
* Content Security Policyを可能な範囲で設定する
* 書籍由来HTMLとアプリ由来JavaScriptの境界を明確にする

## 23. アクセシビリティ

* VoiceOverによる本文読み上げ
* キーボードだけでの操作
* 目次のアウトライン構造
* ボタンとツールバー項目へのラベル付与
* コントラスト設定
* 文字サイズ拡大
* Reduce Motionへの対応
* 高コントラストモードへの対応
* 画像の代替テキスト表示
* 出版物のアクセシビリティメタデータ表示

## 24. MVP

初期リリースには以下を含める。

### 書籍管理

* EPUBを開く
* 最近開いた書籍の一覧
* 表紙と書誌情報
* Security-Scoped Bookmark
* 前回位置の保存

### リフロー型

* 横書き表示
* 縦スクロール
* ページ送り
* 文字サイズ
* 行間
* 本文幅
* 明暗テーマ
* 目次
* 本文内リンク
* 戻る・進む
* テキスト選択とコピー
* 基本検索

### 固定レイアウト／画像型

* 単ページ表示
* 見開き表示
* ページ移動
* 拡大・縮小
* ドラッグ移動
* ページ全体／原寸表示
* サムネイルによるページ移動

### 技術書向け機能

* コードブロックのコピー
* コードの折り返し切り替え
* 図版の拡大
* 外部URLをブラウザで開く
* 脚注表示
* キーボードショートカット

### 注釈

* しおり
* ハイライト
* 簡単なメモ

## 25. MVP後の機能

### 第2段階

* SQLite全文検索インデックス
* 日本語N-gram検索
* 図一覧
* 表の拡大表示
* コードブロック専用ビュー
* 同一書籍の複数ウィンドウ
* Markdown形式での引用コピー
* 注釈のJSON／Markdown書き出し
* Spotlight連携
* 独自URL Scheme

### 第3段階

* 画像型EPUBのローカルOCR
* OCR検索結果からページへ移動
* コード言語の自動判定
* コードのシンタックスハイライト
* 書籍横断検索
* 書籍間リンク
* OPDS
* Readium LCP
* iCloudによる注釈同期
* iPad版

### 第4段階

* ローカルLLMによる選択範囲の説明
* 用語集の自動生成
* 章ごとの要点抽出
* コード例の解説
* 埋め込みによる意味検索
* 書籍横断の質問応答

AI機能は本文表示とは別レイヤーとし、読書開始やページ送りの性能へ影響させない。

## 26. 実装順序

### フェーズ1：表示検証

* Readium Swiftを組み込む
* リフロー型EPUBを表示する
* 固定レイアウトEPUBを表示する
* macOS上でマウスとキーボード操作を検証する
* 日本語技術書の代表的なEPUBを収集する
* 起動、章移動、画像表示を計測する

### フェーズ2：Reader MVP

* ReaderSession
* ウィンドウ管理
* 目次
* 履歴
* 表示設定
* コードコピー
* 画像ビューア
* 固定レイアウト操作
* 前回位置復元

### フェーズ3：ライブラリ

* 書籍データベース
* 表紙キャッシュ
* ファイル変更検出
* 最近開いた書籍
* 検索インデックス

### フェーズ4：注釈

* ハイライト
* しおり
* メモ
* 注釈一覧
* 書き出し

### フェーズ5：性能改善

* Instrumentsによる計測
* Publication解析キャッシュ
* WebViewライフサイクル最適化
* Decorationのbatch化
* 隣接resourceの先読み
* 画像デコード最適化
* JavaScript通信の削減
* Readium Navigatorの必要箇所のみフォーク

## 27. 検証用EPUBセット

実装中は、少なくとも次の種類を用意する。

* 標準的な横書きリフロー型技術書
* コードブロックが多い書籍
* 長い表を含む書籍
* SVG図版を含む書籍
* MathMLを含む書籍
* 画像が多い書籍
* 1章が非常に長い書籍
* spineが細かく分割された書籍
* 固定レイアウトの画像型書籍
* 見開き指定のある書籍
* 右開きの書籍
* EPUB 2由来の書籍
* CSSが複雑な書籍
* 一部リソースが欠落した書籍
* 数百MB規模の書籍

## 28. 成功条件

初期版の成功条件を次のように定める。

* 一般的な日本語技術書を安定して開ける
* テキスト型と画像型を自動判定して適切に表示できる
* コード、表、図版を無理なく確認できる
* 目次、検索、履歴によって素早く往復できる
* アプリ起動後、書籍を開いて読み始めるまでの待ち時間が短い
* ページ送りやスクロールがThorium Desktopより明確に軽快に感じられる
* 書籍を外部へ送らず、ローカルだけで利用できる
* Readiumの更新を継続的に取り込める
* 独自実装部分がReadium本体と明確に分離されている
