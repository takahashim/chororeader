# chororeader 仕様書

macOS 向けの日本語技術書リーダーの仕様を定める。
本書は最初の検討メモ（memo.md）を実現可能性の検討結果で改訂した第 2 版であり、以後はこちらを正とする。

## 1. memo.md からの主な変更点

- Readium Swift Toolkit の採用を取りやめる。
  同 Toolkit は iOS と iPadOS 専用であり、macOS ネイティブでは動作しないため。
  代わりに WKWebView を直接使う自前ナビゲータを実装し、組版ノウハウは Readium CSS（スタイルシート集）の流用で取り込む。
- PDF を対応形式に加え、マルチフォーマット技術書リーダーとして位置づけ直す。
  表示は macOS 標準の PDFKit に任せるため、追加コストが小さい。
- 比較閲覧を中核要件に据える。
  複数箇所を確認する際にページを行き来する負担をなくすことが、このアプリの主要な存在理由である。
  解決手段は複数ウィンドウと、リンクプレビューポップオーバーとする。
- MVP を縮小する。
  リフロー型の表示は縦スクロールのみとし、ページ送りモードは設けないことにした（第 5.4 節）。
  注釈はしおりのみとし、ハイライトとメモは後段に回す。

## 2. 目的と方針

### 2.1 目的

日本語の技術書と技術文書を macOS 上で快適に読むための、軽量かつ高速なリーダーを開発する。
リッチな機能より、起動と表示が速く普通に読めることを優先する。

EPUB と PDF に対応する。
ただし製品の価値は形式の数ではなく、手元の技術文書がどの形式であっても同じ読書体験（目次、検索、比較、位置復元、コピー）に載ることにある。
出版社直販や同人誌は EPUB、出版社 PDF や論文や社内資料は PDF である。
Markdown は将来の検討事項とする（第 8 章）。

技術書特有の閲覧行動のうち、特に次を重視する。

- 複数箇所を並べて比較する
- 目次と検索で素早く移動する
- コードと図表を確認し、コピーする
- 複数の書籍を並行して参照する

### 2.2 製品方針

- macOS ネイティブアプリとして実装する（SwiftUI を中心に、必要な箇所で AppKit を使う）
- EPUB の本文表示は WKWebView による自前ナビゲータで行う
- 表示設定の注入には Readium CSS を流用する
- PDF の表示は PDFKit に任せる
- 読書時にネットワークを使わない（この不変条件を全機能の前提とする）
- 書籍ファイルを外部サービスへ送信しない
- 元ファイルを改変しない（変換や正規化はすべて配信時に非破壊で行う）
- ローカルファイルを中心とした単純な蔵書管理にする

### 2.3 対象外

- 電子書籍ストア、書籍販売、DRM（Readium LCP 含む）
- SNS 機能
- Linux への対応
- iPhone と iPad との同期
- MOBI や AZW など Kindle 系形式
- 文書の編集と形式間の変換
- クラウドへの書籍アップロード
- リモートからの文書取得と同期（フィード購読、リポジトリ追従など）

リモート取得のうち「URL からの単発インポート」（一度ダウンロードしてローカル保存し、以後はローカル文書として扱う）は、読書時ネットワーク不使用の不変条件と両立するため、将来の検討余地として残す。
更新の追従や同期はこの製品の範囲としない。

## 3. 対応形式

書籍を開く際、次の表示種別へ分類し、結果をデータベースへ保存する。

```text
文書
├── リフロー型 EPUB
├── 固定レイアウト EPUB
│   ├── 画像ページ型
│   ├── SVG ページ型
│   └── XHTML 固定配置型
├── PDF
└── 非対応または破損
```

- **リフロー型 EPUB**：横書きの日本語技術書を主対象とする。縦書きは可能な範囲で表示するが、初期版の主対象としない。
- **固定レイアウト EPUB**：spine の各項目がページ画像である形式を主対象とする。XHTML 固定配置型は viewport 指定に基づく拡縮で表示する。
- **PDF**：PDFKit で表示する。スキャン PDF（テキスト層なし）は表示できるが検索不可となり、その旨を明示する。

## 4. ウィンドウモデルと比較閲覧

### 4.1 基本モデル

ブラウザと同じ複数ウィンドウモデルを採用する。

- 1 ウィンドウに 1 文書を表示する
- 同一書籍を複数ウィンドウで開ける
- macOS 標準のウィンドウタブに対応する（独自タブ UI は作らない）
- ウィンドウ配置と表示位置を復元する
- OS のウィンドウタイリングとフルスクリーン Split View で並べられる

分割ペイン UI は初期版では作らない。
並べて読む用途は複数ウィンドウと OS のタイリングで満たせるため、フォーカス管理を自前実装する分割ペインはコストに見合わない。

### 4.2 比較のための導線

複数箇所の比較を、ページの行き来なしで成立させる。

- 内部リンク、目次項目、検索結果、図への参照を `⌘クリック` すると、リンク先を新しいウィンドウで開く
- 新しいウィンドウは可能なら現在のウィンドウの隣（画面の反対半分）へ自動配置する
- 「現在位置を新しいウィンドウで開く」操作を用意する（同じ場所を起点に片方だけ読み進める用途）

### 4.3 書籍単位の共有層

同一書籍を複数ウィンドウで開くため、ウィンドウ横断で共有する **BookDocument** 層を置く。

- パース済みの文書モデルを共有する（二重パースしない）
- しおりと注釈は変更通知つきストアで共有し、全ウィンドウへ即時反映する
- 読書位置は書籍につき 1 つとし、最後にアクティブだったウィンドウの位置を保存する

## 5. リフロー型 EPUB の表示

### 5.1 ナビゲータ

- WKWebView に章の XHTML を表示し、リソースは `WKURLSchemeHandler` で EPUB 内から供給する
- 表示方式は縦スクロールのみとする。ページ送りモードは設けない（第 5.4 節）
- 章末には次の章への行き先を表示し、spine 境界を跨ぐことを目に見える形にする
- スクロール位置の通知は throttle する

### 5.2 表示設定

次を変更できる。

- 文字サイズ、行間、本文幅、ページ余白
- 書体、コード用書体
- 明暗テーマ

設定の注入には Readium CSS を使い、出版社 CSS を壊さない範囲で上書きする。
本文はウィンドウ幅いっぱいに引き伸ばさず、上限幅を設けて中央配置できるようにする。
再レイアウトを伴う設定変更は 100〜200 ミリ秒 debounce する。

### 5.3 出版社 CSS の扱い

1. 出版社スタイルを優先
2. 読みやすさに関する設定だけ上書き（既定）
3. アプリ標準スタイルを優先

いずれの段階でも、コードブロック、表、数式、図版、脚注のレイアウトは不用意に破壊しない。

文字色の上書きは、次の規則に従う。

- 明るいテーマとセピアでは文字色を上書きしない。出版社の配色は明るい紙を前提に組まれており、そのまま読めるためである
- 暗いテーマでは上書きするが、**背景色を持つ要素とその内側には触れない**。見出しの黒帯のように、出版社が背景色と文字色を組にして指定している箇所から文字色だけを奪うと、黒地に暗い文字が残って読めなくなる
- どの要素が背景色を持つかは実行時に判定し、持たない要素にだけ印を付けて色を当てる
- コードブロックは枠にだけ色を当て、内側の色付け（シンタックスハイライト）には触れない

### 5.4 ページ送りモードを設けない理由

リフロー型 EPUB にページ送りモードを用意しない。

技術書ではコードブロックや表がページ境界で分断されると読みにくく、縦スクロールの方が適している。
また、段組によるページ分割を自作すると章境界や分断の品質を確保しにくい。
表示方式が 1 つに定まることで、読書位置の表現も操作の意味もモードによって変わらなくなる。

## 6. CSS 互換レイヤー

古い EPUB の CSS を、配信時に解釈可能へ書き換える。
スキームハンドラを通るリソースを配信の瞬間に変換するため、元ファイルには触れない。

- `-epub-` プレフィックス付きプロパティを標準プロパティへ変換する（`-epub-writing-mode` を `writing-mode` へ、`-epub-text-combine: horizontal` を `text-combine-upright: all` へ、など）
- 変換は簡易 CSS トークナイザを通して行い、コメントや文字列リテラル内の誤置換を防ぐ
- XHTML 内の `<style>` ブロックにも同じ変換を適用する
- UTF-8 以外の文字コードを正規化する
- 固定幅指定の画像と表を `max-width: 100%` で緩和する（第 5.3 節の第 2 段階以降で適用）

変換は「解釈可能にする」ことに限定し、意味を変える書き換えはしない。
変換結果はコンテンツハッシュをキーにキャッシュし、変換内容は書籍ごとにログへ残す。
表示崩れの報告時に、変換が原因か元の CSS が原因かを切り分けるためである。

## 7. 固定レイアウト型 EPUB と PDF の表示

### 7.1 共通の表示操作

- 並べ方：連続スクロール（既定）、単ページ、見開き。技術書 PDF は連続スクロールが読みやすいため既定とする
- 並べ方を変えても操作の意味は変えない（第 10.2 節）
- フィット：ページ全体、横幅、原寸、前回の倍率を維持
- `⌘+` `⌘-` `⌘0` とピンチ操作による拡縮、拡大時のドラッグ移動
- 見開きは出版物メタデータの綴じ方向に従い、ユーザーが上書きできる

### 7.2 画像ページ型 EPUB

- ページを合成した 1 枚の文書として WebView へ渡す。画像の復号と拡大は WebKit に任せる
- ページが画像 1 枚で構成されるかを判定し、そうでないページは元の XHTML をそのまま埋め込む
- 見開きは表紙を単独にし、以降を 2 枚ずつまとめる。綴じ方向が右開きなら左右を入れ替える
- 連続表示では、見えているページだけを復号する
- 表示サイズに対して過剰な解像度でデコードしない
- サムネイル一覧は ImageIO の縮小読み込みで作り、一覧に現れたページの分だけ生成する
- メモリ警告時に画像キャッシュを破棄する

### 7.3 PDF

- PDFView をそのまま使い、描画、拡縮、テキスト選択、リンク注釈、アウトラインを PDFKit に任せる
- 目次はアウトラインから、検索は PDFKit の全文検索から、形式共通のインターフェースへ変換する
- 文字サイズ変更などリフロー前提の設定は PDF では無効にする

## 8. Markdown（将来の検討）

現時点では対応しない。

## 9. 参照確認 UI

比較閲覧（第 4.2 節）を補う、移動なしの確認手段を用意する。

- 脚注リンクはポップオーバーで内容を表示し、本文位置を保つ
- 脚注に限らず、内部リンク全般（章間参照、図表への参照）をポップオーバーでプレビューできる
- ポップオーバーから「ここへ移動」「新しいウィンドウで開く」へ遷移できる

数秒で済む確認はポップオーバー、読み比べは `⌘クリック` で新しいウィンドウ、という二段構えとする。

## 10. ナビゲーションと検索

### 10.1 目次

- 階層目次をアウトライン表示し、現在位置を選択状態にする
- 目次内を絞り込み検索できる
- 目次項目の `⌘クリック` で新しいウィンドウに開く

### 10.2 移動の操作

軸ごとに意味を 1 つに定める。
縦は読むための軸、横は移動するための軸とする。
縦方向の操作で章やページを跨がないことで、読んでいるつもりが移動してしまう状態をなくす。

| 操作 | EPUB | PDF・固定レイアウト |
| --- | --- | --- |
| ↑ / ↓ | 章の中をスクロール | ページの中をスクロール |
| Space / Shift + Space | 1 画面分スクロール | 1 画面分スクロール |
| ← / → | 前の章 / 次の章 | 前のページ / 次のページ |

この対応は並べ方（連続スクロール、単ページ、見開き）によらず変えない。
連続スクロールでは、← / → は隣のページの先頭へ送る。

右開きの出版物では左右の意味を入れ替える。

矢印キーはメニューのショートカットとして登録しない。
メニューのキー等価はファーストレスポンダより優先されるため、目次リストや検索欄でも矢印キーを奪ってしまうからである。
本文ビューがフォーカスを持っているときだけ処理する。

トラックパッドの横スワイプには移動を割り当てない。
横に長いコードブロックや表の横スクロールと衝突するためである。

### 10.3 履歴

ウィンドウごとにブラウザ型の戻る（`⌘[`）と進む（`⌘]`）を持つ。
目次、リンク、検索結果、しおりによる移動を履歴に積み、移動理由を記録する。

### 10.4 検索

- 書籍を開いた窓の検索は、章単位のテキスト抽出による部分一致検索とする
- 画面に出ないもの（head の題名、script、style）は本文として数えない。
  当たっても飛び先に何も無く、探しているものが見つからない
- 検索結果には章名、前後の文脈、種別（本文か見出しかコードか）を表示する
- 検索結果から飛んだ先では、当たった語をその場で強調する
- 書棚では蔵書を横断して引ける
- インデックスは読書開始のクリティカルパスでは作らず、アイドル時に生成する
- テキストを持たない書籍（画像型 EPUB とスキャン PDF）では検索不可であることを明示する

強調は、飛んだ先で目当ての語を探し直さずに済ませるためのものである。
検索結果の一覧に前後の文脈が出ていても、着いた章やページのどこに当たったのかは分からない。
本文が HTML であるもの（リフローと固定レイアウト EPUB）では語そのものを、
PDF では描いた紙面に当たりの矩形を重ねて強調する。
強調は移動のたびに引き直し、その書籍の検索をやめたときに消える。

#### インデックスの持ち方

二字組（バイグラム）を鍵にした転置索引を、書籍ごとに 1 つ持つ。
単位は EPUB なら読み順の 1 項目、PDF なら 1 ページとする。

**索引は候補を絞るだけで、当たりを決めない。**
二字組が同じ単位に出ていても、続けて出ているとは限らないためである。
絞った単位を走査にかけ直して初めて当たりが決まる。
これにより、検索結果は索引の有無で変わらない（契約の「検索のヒット位置、件数、順序」を動かさない）。
当たりの位置を索引に持たないぶん容量も小さく、39 万字・86 章の技術書で 153 KB に収まる。

SQLite FTS5 を採らないのは、trigram のトークナイザが 3 文字未満を引けないためである。
「型」「関数」のような 2 字の語が引けない全文検索は、日本語の技術書では用をなさない。
容量でも不利で、同じ本文を入れた実測では本文なし・位置なしの構成でも 2 倍以上になる。

索引の形式は実装ごとに閉じたもので、契約には載せない。
macOS 版と Tauri 版はファイルを共有しないため、揃えるべきは索引の中身ではなく検索結果である。

### 10.5 書籍の情報

書籍そのものが名乗っていることを出す（⌘I）。誰が書いたか、いつ作られたか、何ページか。

- EPUB は題名、著者、言語、識別子、組み方、綴じ方向、章と目次の数、表紙の有無
- PDF は題名、著者、主題、キーワード、作成したもの、書き出したもの、作成と更新の日時、
  版、ページ数、ページの大きさ（pt と mm）、文字の層の有無、暗号化と許諾
- どちらもファイルの名前、大きさ、更新日時、場所を添える
- 名乗っていない項目は並べない。空欄を出しても読む人の役に立たない
- 書き写せるようにする（画面で見るだけでは写せない）
- 読書の窓からは ⌘I と道具帯の情報ボタン。書棚では右クリック（表紙・一覧・横断検索の当たり）から
- 書棚は開いていない書籍のことも尋ねられる。開かずにファイルから読む。
  開けない書籍でも、ファイルの素性までは出す（何も出ないより手掛かりになる）

**診断（⌘⌥I、第 13.3 節）とは役目が違う。** あちらは表示がおかしいときに原因が
書籍側か実装側かを切り分けるためのもので、実装間の突き合わせにも使う。
こちらは読む人が知りたいことを出すだけで、突き合わせの対象にしない。

PDF の「文字の層」は検索できるかどうかに直結する。入稿用にフォントを
アウトライン化した PDF は見た目が鮮明でも文字を持たず、検索が黙って 0 件になる。

### 10.6 フォルダからまとめて取り込む

蔵書がフォルダに溜まっている状態から始められるようにする。

- 書棚の「＋」から「フォルダを取り込む…」。下の階層も探す（深さ 5 まで）
- 拡張子で拾う（EPUB と PDF）。隠しファイルと、束として扱われるフォルダの中は見ない
- **書籍は開かずに書棚へ加える。** 題名・著者・形式・表紙だけを取り出して並べる
- 既に並んでいる書籍は触らない。読書位置も並び順も、取り込みで動かさない
- 取り込んだ書籍の並びの基準は**ファイルの更新日時**とする。
  取り込んだ時刻にすると、何百冊が「いま開いた」ことになって書棚の先頭を埋め、
  本当に最近読んだ本が押し出される
- 1 冊ごとに手を離し、進み具合を見せ、途中でやめられるようにする。
  1 冊あたり 35 ミリ秒ほどかかるので、1,700 冊では 1 分ほどになる

### 10.7 書棚の掃除

書類が紛れ込む。PDF は書籍とは限らず、請求書も申請書もスキャンした紙も同じ形をしている。
拡張子とメタデータからは見分けられない（手元の蔵書では 241 冊中 229 冊に著者が無く、
231 冊は題名がファイル名のままだった）。**自動で省くことはしない。人が外せるようにする。**

- 表でも表紙でも複数選べる。表は ⇧ と ⌘、表紙は ⌘ クリック
- ⌘A ですべて選択、⌫ で外す
- 何冊選んでいるかを常に出す。見た目だけでは数えられない
- 外す前に必ず尋ねる。**書棚から外すだけでファイルは消えないが、読書位置としおりは失われる**

## 11. 選択とコピーと技術書向け操作

- テキスト選択とプレーンテキストのコピー
- コードブロック単位のコピー（ホバー時に右上へコピーボタンを表示する）
- コードの行折り返し切り替えと横スクロール
- 画像のクリックによる拡大表示（オーバーレイ）、原寸表示、コピー
- 本文中の URL を既定ブラウザで開く（開く前に URL をステータス表示する)
- 引用情報付きコピーと Markdown コピーは第 2 段階とする

## 12. 読書位置と注釈

### 12.1 位置表現

形式共通の位置表現を定義する。

- EPUB：spine 項目の href、章内 progression、CSS selector、周辺テキスト
- PDF：ページ番号とページ内座標、表示倍率

復元は詳細な手がかりから順に試行し、失敗したら段階的に粗い位置へフォールバックする。

### 12.2 しおり

- 読書位置に対して付与し、名前を変更できる
- 章ごとに一覧表示し、キーボードショートカットで追加する

ハイライトとメモは第 2 段階とする。
ただしストア設計（第 4.3 節の共有層）は初期版から注釈を格納できる形にしておく。

## 13. アーキテクチャ

### 13.1 実装の分担

実装は 2 つある。
どちらも本命であり、OS ごとにその OS のやり方で作る。

- **macOS 版**：Swift、SwiftUI、WKWebView、PDFKit（`macos/`）
- **Windows 版**：Rust、Tauri、システムの WebView、MuPDF（`tauri/`）

macOS では Windows 版を使わない。
ネイティブのほうが操作の感触が OS に揃い、PDFKit と WKWebView の作り込みをそのまま活かせる。

Windows でネイティブを選ばなかったのは、開発と検証を macOS の上で回せることを取ったためである。
これは利用者に関係のない開発都合であり、Windows 版の位置づけを下げるものではない。
判断の根拠は spikes/findings-tauri.md にある。

**画面の機能は 2 回作ることになる。**
それを承知の上で選んでいる。
入力から出力が一意に決まる部分だけを第 13.3 節の契約で機械的に揃え、
画面の作りは各実装がその OS に合わせて決める。

Windows 版はいったん C# で書き、契約を通したうえで Tauri へ移した。
移り終えたあとも契約の相手として残していたが、
実装を 3 つ抱えると契約を変えるたびに 3 つ直すことになるため畳んだ。
経緯は spikes/findings-windows.md にある。

### 13.2 macOS 版の構成

```text
SwiftUI App
├── Library
│   ├── BookRepository
│   ├── FileAccess（Security-Scoped Bookmark、フォルダスコープ含む）
│   └── CoverCache
├── Document
│   ├── BookDocument（書籍ごとに 1 つ、ウィンドウ横断で共有）
│   ├── EPUBParser（container / OPF / 目次 / spine）
│   ├── PDFDocument（PDFKit）
│   └── AnnotationStore
├── Reader
│   ├── ReaderWindow（WindowGroup、書籍 ID と Locator をパラメータに開く）
│   ├── ReaderSession（ウィンドウごとに 1 つ：位置、履歴、表示状態）
│   ├── WebNavigator（WKWebView + WKURLSchemeHandler + CSS 互換レイヤー）
│   └── PDFNavigator（PDFView）
├── Search
│   ├── ContentExtractor
│   └── SearchIndexer（第 2 段階）
└── Persistence
    └── SQLite
```

- ReaderSession は SwiftUI View の再生成で作り直されないようにする
- EPUB パーサは自前実装とする。フェーズ 0 のスパイクで、ReadiumShared と ReadiumStreamer は UIKit 依存（公開 API への UIImage 露出を含む）により macOS でビルド不成立と確認した（spikes/findings.md）
- ナビゲータは目次、検索、位置表現を形式共通のプロトコルで提供し、サイドバーと履歴とライブラリは形式を意識しない。新形式の追加コストが「ナビゲータと形式判定」に閉じる構造を保つ

### 13.3 実装間の契約

2 つの実装が同じ振る舞いをすることを、機械的に確かめる。
取り決めと検証の道具は `conformance/` にある。

揃えるのは、入力から出力が一意に決まる部分だけである。
EPUB の解析結果、パスの解決規則、CSS 互換レイヤーの変換結果、本文の抽出、検索の位置、
形式判定とエラー分類、書籍の診断、表示設定から作る CSS、抜粋の切り出し範囲、
固定レイアウトのページ種別と見開きの組み方が対象になる。

本文の描画結果と画面の作りは揃えない。
WKWebView とシステムの WebView は別物であり、揃えようとすると両方が窮屈になる。

各実装は `probe` という CLI を生やし、同じ入力に対する出力を JSON で返す。
凍結した期待値との照合と、実装どうしの突き合わせの両方をこれで行う。

## 14. データモデル

```text
Book
├── id
├── fileBookmark（Security-Scoped Bookmark）
├── fileSize / fileModificationDate / contentHash
├── format（reflowEPUB / fixedEPUB / pdf）
├── title / authors / language / identifier
├── coverCacheKey
├── lastOpenedAt
├── lastPosition
└── indexingStatus

Annotation（第 2 段階で使用、スキーマは初期版から用意）
├── id / bookID / kind（bookmark / highlight / note）
├── positionJSON
├── selectedText / note / category
└── createdAt / updatedAt

NavigationHistoryEntry
├── bookID / positionJSON / title
├── reason（manual / toc / search / internalLink / footnote / bookmark）
└── createdAt
```

元ファイルが変更された場合はキャッシュ（パース結果、表紙、検索インデックス）を再生成し、しおりは可能な範囲で再対応付けする。

## 15. セキュリティ

- 読書時にネットワークを使わない（第 2.2 節の不変条件）
- EPUB 内の JavaScript は無効化する。書籍由来の JavaScript は動かさない
- WebView からの外部ネットワークアクセスを禁止し、外部 URL は OS のブラウザで開く
- スキームハンドラの供給対象を当該アーカイブ内に限定し、パスを正規化して検証する
- ZIP bomb と過度な展開サイズを検出する
- 書籍由来の HTML とアプリ由来の注入 JavaScript の境界を明確にする

### 15.1 書籍の JavaScript を止める仕組み

止め方は実装で違う。**どちらも「書籍の script は走らず、こちらの注入だけが走る」に落とす。**

**macOS 版**は本文専用の WKWebView を持つので、webview 単位で切れる。
`allowsContentJavaScript = false` にしても `WKUserScript` は走るという WebKit の性質に乗り、
注入したスクリプトだけが動く。窓との通信は `WKUserContentController` の
メッセージハンドラ（native bridge）1 本に限る。

**Tauri 版**は画面と本文が同じ WebView にいる（本文は iframe）。
webview 単位で切ると画面自体が死ぬので、枠ごとに決める。

- 枠は `sandbox="allow-scripts"`。**`allow-same-origin` は与えない。**
  本文は生成元を持たない文書になり、書籍の側からアプリの生成元へ手が届かない。
  `allow-top-navigation` と `allow-popups` と `allow-forms` も与えない
- 走ってよい script は、配信時の CSP `script-src 'nonce-…'` で 1 本に限る。
  nonce は要求ごとに引き直すので、書籍側に書いておいて当てることはできない
- 本文の HTML を解析して `<script>` を削る形は**採らない**。
  `<svg><script>`、`on…=` 属性、`javascript:` URL を取りこぼす。
  CSP はブラウザが強制するので、書き方の変化に左右されない
- ネットワークも同じ CSP で塞ぐ。`default-src 'none'` を土台に、
  `img-src` `media-src` `font-src` `style-src` を独自スキームだけに許す。
  `connect-src 'none'`、`base-uri 'none'`、`form-action 'none'`
- 生成元を持たない相手なので、窓からは本文の DOM に触れない。
  触る仕事は出先が引き受ける（第 15.2 節）

### 15.2 本文の出先と窓の言葉（Tauri 版）

本文に触るのは、本文の中で動く出先ただ 1 つとする（`tauri/app/ui/agent.js`）。
窓とは postMessage で話し、やり取りする言葉をここに定める。
**相手の見分けに `event.origin` を使ってはならない。**
生成元を持たない相手であり、独自スキームでは中身が処方どおりにならない。
窓は `event.source` が本文の枠かどうかで、出先は `event.source === parent` で見分ける。

窓 → 出先（言いつけ）

| | すること |
|---|---|
| `style` | 表示設定の CSS を当てる。暗いテーマの文字色付けもここ |
| `dress` | 本文に無いものを足す。図版の拡大、コードのコピー、章末の行き先 |
| `go` | 覚えていた場所へ戻す。飛び先、書き出し、割合の順に試す |
| `top` | 先頭へ |
| `by` | 送る。量と、1 画面ぶんかどうか |
| `zoom` | 倍率を当てる |
| `approach` | 配られた本文に入っている印まで送る |
| `unmark` | 印の包みを解く |
| `report` | 中の様子を伝える（動作確認が見る唯一の覗き口） |
| `press` | 本文の中でキーを押してみせる（動作確認が鍵盤の経路を通すため） |

出先 → 窓（出来事）

| | 意味 |
|---|---|
| `ready` | 入り終わった。窓はこれで「目当ての文書が入ったか」を見る |
| `at` | 居場所が動いた。割合と、画面の上端にある文字 |
| `link` | 本文のリンクが押された。行き先を決めるのは窓 |
| `image` | 図版が押された |
| `copy` | コードのコピーが押された。写すのは窓（生成元を持たない文書から clipboard は触れない） |
| `next` | 章末の行き先が押された |
| `key` | 本文の中でキーが押された |
| `wheel` | ピンチ（Ctrl を伴う wheel） |
| `stumbled` | 言いつけの途中で転んだ |
| `report` | `report` への返事 |

居場所は**出先から押し出す**。窓の `position()` は同期で答える約束であり、
尋ねて待つ形にすると履歴と読書位置がすべて往復を待つことになるためである。

出先はモジュールにできない。module の取得は CORS を伴い、生成元を持たない文書からは読めない。

## 16. 性能

### 16.1 目標値

Apple Silicon Mac 上のローカルファイルを前提に、次を設計目標とする（保証値ではない）。

- 登録済み書籍の本文表示：1 秒以内
- 章移動：300 ミリ秒以内に応答開始
- 検索結果の初回表示：200 ミリ秒以内
- スクロール：60fps
- UI 操作中にメインスレッドを 100 ミリ秒以上連続占有しない

成功条件として、Thorium Desktop（Electron 製）より明確に軽快であることを確認する。

### 16.2 クリティカルパス

読書開始は「ファイル参照の復元、パース、前回位置の読み込み、ナビゲータ表示」のみで構成する。
検索インデックス生成、全文抽出、サムネイル生成、図一覧生成は本文表示後のアイドル時へ遅延する。
`os_signpost` で起動、パース、初回表示、章移動、画像デコードを計測する。

## 17. MVP

### 書籍管理

- EPUB と PDF を開く（Finder、ドロップ、ファイルダイアログ）
- 最近開いた書籍の一覧、表紙と書誌情報
- Security-Scoped Bookmark による参照保持と前回位置の復元

### 表示

- リフロー型 EPUB の縦スクロール表示
- 文字サイズ、行間、本文幅、明暗テーマ
- CSS 互換レイヤー
- 固定レイアウト EPUB の単ページと見開き表示、拡縮、ドラッグ移動、サムネイル
- PDF 表示（PDFKit：表示、拡縮、目次、検索、テキスト選択）

### 比較と移動

- 複数ウィンドウと OS 標準ウィンドウタブ
- `⌘クリック` で新しいウィンドウに開く（リンク、目次、検索結果）
- 脚注と内部リンクのプレビューポップオーバー
- 目次、戻ると進む、章単位の部分一致検索

### 技術書向け操作

- コードブロックのコピーと折り返し切り替え
- 図版の拡大表示
- 外部 URL をブラウザで開く
- キーボードショートカット

### 注釈

- しおり

## 18. MVP 後

### 第 2 段階

- ハイライトとメモ、注釈一覧、注釈の書き出し
- 引用情報付きコピーと Markdown コピー
- 図一覧と表の拡大表示
- 図とコードのフローティングパネル（常に手前に表示する小ウィンドウ）
- 新しいウィンドウの自動隣接配置
- Spotlight 連携と独自 URL Scheme

- Markdown 対応（第 8 章）

### 第 3 段階

- Vision Framework によるローカル OCR（画像型 EPUB とスキャン PDF の検索）
- コードのシンタックスハイライト（EPUB 向け。出版社装飾がない場合に限る）
- 分割ペイン UI（複数ウィンドウ運用で不足が確認された場合のみ）
- URL からの単発インポート（要否を利用実態で判断する。採用時も読書時ネットワーク不使用は維持する）

## 19. 実装順序

### フェーズ 0：スパイク

- WKWebView と `WKURLSchemeHandler` で手元の EPUB の章を表示し、日本語組版、コードブロック、画像の見え方と速度を確認する
- ReadiumShared と ReadiumStreamer の macOS ビルド可否を確認し、パース層の自前実装か流用かを決める
- PDFView で技術書 PDF を表示し、アウトラインと検索の取得を確認する

### フェーズ 1：Reader MVP

- EPUB パースとナビゲータ、縦スクロール表示、表示設定、CSS 互換レイヤー
- PDF ナビゲータ
- ウィンドウ管理と BookDocument 共有層、`⌘クリック` 導線
- 目次、履歴、簡易検索、前回位置復元

### フェーズ 2：ライブラリと仕上げ

- 書籍データベース、表紙キャッシュ、ファイル変更検出
- しおり
- プレビューポップオーバー
- 計測と性能改善

## 20. 検証用書籍セット

- 標準的な横書きリフロー型技術書
- コードブロックが多い書籍
- 長い表を含む書籍
- SVG 図版と MathML を含む書籍
- 1 章が非常に長い書籍と、spine が細かく分割された書籍
- EPUB 2 由来の古い CSS を持つ書籍（`-epub-` プレフィックス検証用）
- 画像型固定レイアウト書籍（見開き指定と右開きを含む）
- テキスト層のある技術書 PDF とスキャン PDF
- 一部リソースが欠落した EPUB
- 数百 MB 規模の書籍

## 21. リスク

- **MathML**：WebKit の対応は実用水準だが、複雑な数式は崩れることがある。検証セットで許容範囲を確認する。
- **縦書き**：描画は WebKit が行うが、スクロール方向の制御は追加実装が要る。初期版は横書き主対象として明示する。
- **ハイライトの復元**：DOM 上の位置の永続化と復元は難所のため第 2 段階に回した。位置表現（第 12.1 節）を初期版から周辺テキスト込みで設計し、後付けを可能にしておく。

## 22. 成功条件

- 一般的な日本語技術書の EPUB と PDF を安定して開ける
- 形式を自動判定して適切に表示できる
- `⌘クリック` とウィンドウタイリングで、複数箇所の比較がページの行き来なしで成立する
- コードと図表を無理なく確認しコピーできる
- 書籍を開いて読み始めるまでの待ち時間が短く、スクロールが Thorium Desktop より明確に軽快である
- 書籍を外部へ送らず、読書時はネットワークなしで完結する
