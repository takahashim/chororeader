# Tauri 版（Windows を主対象とするが、コードは OS に依らない）。
# まず契約を通すため、UI 非依存の core と、突き合わせ用の probe から作る。
[workspace]
members = ["core", "probe"]
resolver = "2"

[workspace.dependencies]
serde_json = "1"
regex = "1"
zip = "2"
quick-xml = "0.37"
unicode-normalization = "0.1"
encoding_rs = "0.8"
